import { FiltroInputPipe } from './filtro-input.pipe';

describe('FiltroInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroInputPipe();
    expect(pipe).toBeTruthy();
  });
});
