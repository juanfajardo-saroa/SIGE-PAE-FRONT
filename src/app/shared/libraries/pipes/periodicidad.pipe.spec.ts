import { PeriodicidadPipe } from './periodicidad.pipe';

describe('PeriodicidadPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodicidadPipe();
    expect(pipe).toBeTruthy();
  });
});
