import { SemaforoContratosPipe } from './semaforo-contratos.pipe';

describe('SemaforoContratosPipe', () => {
  it('create an instance', () => {
    const pipe = new SemaforoContratosPipe();
    expect(pipe).toBeTruthy();
  });
});
